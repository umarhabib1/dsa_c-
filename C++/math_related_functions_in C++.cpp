#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double x = 3.9;
    double y = 4;
    double z;

    // z = std::max(x,y);
    // z = std::min(x,y);
    // z = pow(x,2);
    // z = pow(5,2);
    // z = sqrt(9);
    // z = sqrt(8);
    // z =abs(-99);
    // z = round(x);
    // z = ceil(x);
    // z = floor(x);
    cout << z;
}