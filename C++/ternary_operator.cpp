#include <iostream>
using namespace std;

int main(){
    // ternary operator  ?   :

    // int age =12;
    // age >= 18 ? cout<<"you can drive" : cout<< "you can not drive";

    // int num = 10;
    // num%2 == 0 ? cout<<"even number" : cout<<"odd number";
    
    int num = 10;
   cout<<(num%2 == 0 ? "even number" : "odd number");
   
   return 0;
    
}